import { Component } from '@angular/core';
import { FeedComponents } from '../../components/feed/main/feed-components';

@Component({
  selector: 'app-feed',
  imports: [FeedComponents],
  templateUrl: './feed.html',
  styleUrl: './feed.css',
})
export class Feed {
// Lógica da página de feed pode ser adicionada aqui
}
