import { Component } from '@angular/core';
import { FeedComponents } from '../../components/feed-components/feed-components';

@Component({
  selector: 'app-feed',
  imports: [FeedComponents],
  templateUrl: './feed.html',
  styleUrl: './feed.css',
})
export class Feed {

}
